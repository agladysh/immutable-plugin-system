{
  "name": "immutable-plugin-system-events-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Example CLI demonstrating immutable-plugin-system",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.13.1",
  "dependencies": {
    "immutable-plugin-system": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
